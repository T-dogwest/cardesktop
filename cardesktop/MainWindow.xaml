<Window x:Class="cardesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cardesktop"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel >
            <Button Content="Törlés" Margin="10"  HorizontalAlignment="Left" Click="DELETE_CLICK"/>
        </StackPanel>
        <DataGrid Grid.Row="1" x:Name="dataGrid" Margin="10" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                <DataGridTextColumn Header="Rendszám" Binding="{Binding License_plate_number}"/>
                <DataGridTextColumn Header="Márka" Binding="{Binding Brand}"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding Model}"/>
                <DataGridTextColumn Header="Napidíj" Binding="{Binding Daily_cost}"/>
                </DataGrid.Columns>
            </DataGrid>
        
    </Grid>
</Window>
